// Compatibility entrypoint. The v2 shelf intentionally restores generation
// state only from embedded PNG metadata; manifest.json is catalog/UI metadata.
export * from "./h3studio_demos_metadata.js";
